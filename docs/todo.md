- Зависимости: 0.1

## Фаза 1 — Базовая модель данных и хранение (CSV) (3–7 дней)

- [ ] **1.1** Определить модели (dataclasses)
- Зависимости: 0.1, 0.2
- Подзадачи:
  - [ ] 1.1.1 `Transaction` (datetime, float amount, str type, str category, str|None subcategory, str account, str|None description)
  - [ ] 1.1.2 `Account` (name, type, currency="RUB", initial_balance=0.0)

- [ ] **1.2** Реализовать хранение в CSV
- Зависимости: 1.1
- Подзадачи:
  - [ ] 1.2.1 `append_transaction_to_csv(tx: Transaction, path: str)`
  - [ ] 1.2.2 `load_transactions_from_csv(path: str) → list[Transaction]`
  - [ ] 1.2.3 Обработка создания файла + заголовков

- [ ] **1.3** config.py с константами
- Зависимости: 0.2
- Подзадачи:
  - [ ] 1.3.1 Определить пути: TRANSACTIONS_CSV = "data/transactions.csv" и т.д.

## Фаза 2 — GUI на customtkinter (основной интерфейс) (7–14 дней)

- [ ] **2.1** Создать базовое окно приложения
- Зависимости: 0.1, 1.3
- Подзадачи:
  - [ ] 2.1.1 В `gui.py` создать класс `App(customtkinter.CTk)`
  - [ ] 2.1.2 Настроить appearance_mode="system", default_color_theme="blue"
  - [ ] 2.1.3 Добавить заголовок, минимальный размер окна (min 900×600)

- [ ] **2.2** Главный дашборд (Tabview или Frame)
- Зависимости: 2.1
- Подзадачи:
  - [ ] 2.2.1 Использовать `CTkTabview` с вкладками: "Дашборд", "Транзакции", "Отчёты", "Цели"
  - [ ] 2.2.2 На "Дашборд" — текущие балансы по счетам, последние 5–10 транзакций, прогресс целей (позже)

- [ ] **2.3** Форма добавления транзакции
- Зависимости: 2.1, 1.1, 1.2
- Подзадачи:
  - [ ] 2.3.1 Создать отдельный `CTkFrame` или `CTkToplevel` для формы
  - [ ] 2.3.2 Поля: CTkEntry (сумма, описание), CTkComboBox (тип: Доход/Расход, счёт, категория), CTkDatePicker или Entry+кнопка (дата)
  - [ ] 2.3.3 Кнопка "Сохранить" → создать Transaction → сохранить в CSV → обновить таблицу/балансы

- [ ] **2.4** Таблица транзакций (CTkScrollableFrame + labels или Treeview)
- Зависимости: 2.3
- Подзадачи:
  - [ ] 2.4.1 Отображать список транзакций (загружать из storage)
  - [ ] 2.4.2 Добавить кнопки Редактировать / Удалить (пока заглушки)

## Фаза 3 — Базовые отчёты и визуализация (5–10 дней)

- [ ] **3.1** Подсчёт балансов по счетам
- Зависимости: 1.2
- Подзадачи:
  - [ ] 3.1.1 Функция `get_balances() → dict[str, float]`
  - [ ] 3.1.2 Отображать на дашборде в CTkLabel или CTkFrame с карточками

- [ ] **3.2** Отчёты за период + графики
- Зависимости: 3.1
- Подзадачи:
  - [ ] 3.2.1 Выбор периода (CTkComboBox: месяц, квартал, год, всё)
  - [ ] 3.2.2 Суммы доход/расход/итого
  - [ ] 3.2.3 Pie-chart расходов по категориям (matplotlib + FigureCanvasTkAgg внутри CTkCanvas/Frame)

## Фаза 4 — Импорт CSV (самая сложная часть MVP, 8–15 дней)

- [ ] **4.1** Кнопка "Импорт CSV" → диалог выбора файла
- Зависимости: 2.x
- [ ] **4.2** Предпросмотр DataFrame в таблице (customtkinter + pandas)
- [ ] **4.3** Интерактивный маппинг колонок (ComboBox для каждой нужной колонки)
- [ ] **4.4** Парсинг → создание Transaction → сохранение + дедупликация

## Фаза 5 — Сборка в исполняемый файл с PyInstaller (после работающего GUI)

- [ ] **5.1** Первая сборка (рекомендуется --onedir)
- Зависимости: весь GUI работает
- Подзадачи:
  - [ ] 5.1.1 Создать spec-файл или использовать команду:
    ```bash
    pyinstaller --onedir --windowed \
      --add-data "path/to/site-packages/customtkinter;customtkinter" \
      --name "FinanceTracker" \
      main.py